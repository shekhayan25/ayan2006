-- 2

-- Write a SELECT query to display only the song\_name and artist from the Playlist table, but show the artist column as 'Singer' using an alias.

SELECT song_name, artist AS Singer
FROM Playlist;
